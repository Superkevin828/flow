/**
 * FlowSmart Configuration
 * ─────────────────────────────────────────────
 * Change API_BASE_URL here once — every page uses it automatically.
 *
 * Development:  'http://localhost:5000/api'
 * Production:   'https://flowsmart-api.onrender.com/api'
 */
const CONFIG = {
  API_BASE_URL: 'http://localhost:5000/api',
};